Your Daily Order Report for <%= @tix.organization.name %>.
<%= @tix.date.to_s(:long) %>

<% if ! @tix.rows.empty? %>
<%= @tix.to_table.to_table(first_row_is_head: true, last_row_is_foot: true) %>
<% else %>
There were no orders placed.
<% end %>

<% if ! @donations.rows.empty? %>
<%= @donations.to_table.to_table(first_row_is_head: true, last_row_is_foot: true) %>
<% elsif @donations.organization.has_kit?("RegularDonationKit") %>
There were no donations made.
<% end %>

View this email in an HTML-capable email reader for links to the aforementioned orders.
