Your Daily Report for <%= @date.to_s(:long) %>.

<% @orders.each do |order| %>
  #<%= order.id %> - <%= pluralize order.tickets.count, "ticket" %> - <%= number_to_currency(order.total.to_f/100) %>
  <%= order_url(order) %>
<%- end %>
